name: bci_management_system
description: BCI integrated student management and payroll Flutter MVP.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.5

flutter:
  uses-material-design: true
